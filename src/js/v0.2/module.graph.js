define([], function() {
	'use strict';

	var api;

	function getLoops(arrays, markedVertexes) {
		var graph = convertToGraph(arrays, markedVertexes);
	}

	function convertToGraph(arrays, markedVertexes) {

	}

	function getTrappedDots(arrays) {

	}

	api = {
		getLoops: getLoops,
		getTrappedDots: getTrappedDots
	};
	return api;
});